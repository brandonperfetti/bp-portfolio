pnpm-lock.yaml
# Generated artifacts — keep generator output byte-exact (CI diff-checks types)
src/payload-types.ts
"src/app/(payload)/admin/importMap.js"
# pnpm eval:matrix writes a local, git-ignored run snapshot (see evals/.gitignore);
# prettier only reads the root ignore files, so it needs its own line here.
evals/corvus-matrix.json
