pnpm-lock.yaml
# Generated artifacts — keep generator output byte-exact (CI diff-checks types)
src/payload-types.ts
"src/app/(payload)/admin/importMap.js"
# pnpm eval:matrix and pnpm eval:ci each write a local, git-ignored run
# snapshot (see evals/.gitignore); prettier only reads the ROOT ignore files,
# so a nested .gitignore does not reach it and each needs its own line here.
evals/corvus-matrix.json
evals/corvus-eval-run.json
